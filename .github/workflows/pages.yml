name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.x"
      - run: pip install -r requirements.txt
      - run: python3 cli.py build
      - name: Assemble site (web/ at the root, data/ + book/ alongside it)
        run: |
          mkdir -p _site
          cp -r web/. _site/
          rm -rf _site/data _site/book   # drop the local-dev symlinks (or their dereferenced copies)
          mkdir -p _site/data
          cp data/books.json _site/data/
          cp -r data/covers _site/data/covers
          cp -r data/book _site/book
      - uses: actions/upload-pages-artifact@v5
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
